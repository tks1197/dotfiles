name: test
on:
  - push
jobs:
  test:
    name: test
    runs-on: macos-latest
    timeout-minutes: 300
    steps:
      - uses: actions/checkout@v4
      - name: Install dotfiles
        run: sh -c "$(curl -fsLS chezmoi.io/getlb)" -- init --apply --verbose tk09291197
