name: test
on:
  - push
jobs:
  test:
    name: test
    runs-on: macos-latest
    timeout-minutes: 300
    steps:
      - uses: actions/checkout@v4
      - name: Install dotfiles
        run: curl -fsLS chezmoi.io/get | sh -s - -b ~/.local/bin init --apply --verbose .
